## 0.1.0 (unreleased)

* Initial release: `TikTokOAuthClient` with `exchangeCode` (PKCE), `refresh`,
  `revoke` and `getUserInfo`, typed `TikTokTokens`, `TikTokUser`,
  `TikTokUserField` and `TikTokApiException`.
